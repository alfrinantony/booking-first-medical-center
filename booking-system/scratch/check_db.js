const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { try { const bookings = await prisma.booking.findMany({ where: { sbProviderName: 'Kristina Garcia' }, select: { sbId: true, patientName: true, doctorId: true, date: true } }); console.log(bookings); } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } check();
