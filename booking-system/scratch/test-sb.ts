require('@next/env').loadEnvConfig('.'); import { getAdminClientList, getAdminBookings, callAdmin } from '../lib/simplybook-client'; async function run() { console.log('COMPANY:', process.env.SIMPLYBOOK_COMPANY_LOGIN); console.log('API_KEY:', process.env.SIMPLYBOOK_API_KEY); try { const clients = await callAdmin('getClientList', [{}]); console.log('Clients count:', Array.isArray(clients) ? clients.length : (clients ? Object.keys(clients).length : 0)); } catch (e) { console.error('Client error:', e); } } run();
