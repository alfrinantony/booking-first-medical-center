const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { try { const sbNames = await prisma.booking.groupBy({ by: ['sbProviderName'], where: { sbProviderName: { contains: 'kristina', mode: 'insensitive' } }, _count: true }); console.log(sbNames); } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } check();
