const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { try { const blob = await prisma.blobStore.findUnique({ where: { key: 'clinics_data' } }); const clinics = JSON.parse(blob.data); for (const c of clinics) { for (const d of c.departments) { for (const doc of d.doctors) { if (doc.name.toLowerCase().includes('kristina')) { console.log(doc); } } } } } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } check();
