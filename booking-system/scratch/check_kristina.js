const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { try { const count = await prisma.booking.count({ where: { doctorId: 'doc-1774181435063' } }); console.log('Bookings for doc-1774181435063:', count); const sbNames = await prisma.booking.groupBy({ by: ['sbProviderName'], where: { doctorId: 'doc-1774181435063' }, _count: true }); console.log(sbNames); } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } check();
