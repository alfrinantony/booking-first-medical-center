const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { try { const blob = await prisma.blobStore.findUnique({ where: { key: 'clinics' } }); if (!blob) { console.log('blob not found'); return; } const clinics = typeof blob.data === 'string' ? JSON.parse(blob.data) : blob.data; for (const c of clinics) { for (const d of c.departments) { for (const doc of d.doctors) { if (doc.name.toLowerCase().includes('kristina') || doc.name.toLowerCase().includes('garcia')) { console.log(doc); } } } } } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } check();
