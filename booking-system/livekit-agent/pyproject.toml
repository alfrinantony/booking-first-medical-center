[project]
name = "livekit-agent"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "livekit-agents[liveavatar,openai,silero,turn-detector]~=1.4",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.2",
]
