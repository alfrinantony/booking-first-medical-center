const fs = require('fs'); const files = ['app/api/admin/services/route.ts', 'app/api/admin/drafts/route.ts', 'app/api/admin/doctors/route.ts', 'app/api/admin/inventory/route.ts', 'app/api/admin/medicines/route.ts']; for (const file of files) { if (!fs.existsSync(file)) continue; let content = fs.readFileSync(file, 'utf8'); if (!content.includes('fetchCache')) { content = content.replace(/export const dynamic = 'force-dynamic';/, \\\export const dynamic = 'force-dynamic';\\\\nexport const fetchCache = 'force-no-store';\\\); fs.writeFileSync(file, content); } }
